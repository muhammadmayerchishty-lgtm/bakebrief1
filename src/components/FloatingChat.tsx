import React, { useState } from "react";`nimport ChatBot from "./ChatBot";`nimport { MessageSquare, X } from "lucide-react";`n`nexport default function FloatingChat() {`n  const [isOpen, setIsOpen] = useState(false);`n  return (`n    <div className="fixed bottom-6 right-6 z-50">`n      {!isOpen ? (`n        <button onClick={() => setIsOpen(true)} className="bg-amber-500 hover:bg-amber-600 text-black font-bold px-5 py-3 rounded-full shadow-2xl flex items-center gap-2 transition-all transform hover:scale-105">`n          <MessageSquare size={20} />`n          <span>Chat with Brief</span>`n        </button>`n      ) : (`n        <div className="relative w-80 sm:w-96 h-[520px] bg-neutral-900 border border-amber-500/30 rounded-2xl shadow-2xl overflow-hidden flex flex-col">`n          <button onClick={() => setIsOpen(false)} className="absolute top-3 right-3 text-neutral-400 hover:text-white z-10 p-1">`n            <X size={20} />`n          </button>`n          <ChatBot />`n        </div>`n      )}`n    </div>`n  );`n}
